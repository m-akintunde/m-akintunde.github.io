name: mysite
version: 0.1.0.0
dependencies:
  - base >= 4.7 && < 5
  - hakyll >= 4.16

executables:
  site:
    main: site.hs
    source-dirs: .
    ghc-options: -threaded -rtsopts -with-rtsopts=-N
    dependencies:
      - base
      - hakyll
